spring:
 application:
   name: item
server:
  port: ${service.server.port:8081}
eureka:
  client:
    serviceUrl:
      defaultZone: ${eureka.serviceUrl:http://127.0.0.1:7001/eureka/}

feign:
  hystix:
    enabled: true